# Commits to skip in `git blame`. GitHub reads this file automatically;
# locally run: git config blame.ignoreRevsFile .git-blame-ignore-revs

# Squash merge of PR #3 (lint ratchet + whole-tree ruff format). The original
# format-only commit 663a0aa was squashed into this one, so this is the SHA
# that carries the reformatting.
b6757c273c4238cf9da39106f44106368564643c
